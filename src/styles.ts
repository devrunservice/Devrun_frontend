import { styled } from "styled-components";

export const AppSection = styled.div`
  display: flex;
  padding: 4rem 0;

  .left-panel {
    flex: 3;
  }

  .right-panel {
    flex: 7;
  }
  width: 1200px;
  margin: 0 auto;
`;
