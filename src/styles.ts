import { styled } from "styled-components";

export const AppSection = styled.div`
  display: flex;

  .left-panel {
    flex: 3;
    padding-top: 2rem;
    padding-bottom: 3rem;
  }

  .right-panel {
    flex: 7;
  }
  width: 1200px;
  margin: 0 auto;
`;
